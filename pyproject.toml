[project]
name = "final-project"
version = "0.1.0"
description = "Ray cluster autoscaler with dynamic worker scaling"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ray[default]==2.55.1",
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.30.0",
    "sse-starlette>=2.1.0",
]

[dependency-groups]
local = [
    "docker>=7.1.0",
    "ray[client]==2.55.1",
    "requests",
]
